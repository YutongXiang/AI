const colors=["#ff5b2e","#2c6bed","#7b43d6"];
const value=(v,d)=>v==null?"无":d==null?String(v):Number(v).toFixed(d);
const rank=(r,t)=>r==null?"无":`${r}/${t??"无"}`;
const time=v=>v?new Intl.DateTimeFormat("zh-CN",{timeZone:"Asia/Shanghai",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit",hour12:false}).format(new Date(v)):"无";
function card(item,i){return `<article class="card" style="--color:${colors[i]}"><div class="card-head"><span class="index">0${i+1}</span><div><p>阶段</p><h2>${item.stage}</h2></div></div><div class="current"><p class="eyebrow">当前成绩</p><div class="rank"><span>排名 / 总有效人数</span><strong>${rank(item.currentRank,item.currentTotal)}</strong></div><div class="score"><span>分数</span><strong>${value(item.currentScore,4)}</strong></div></div><div class="best"><div><small>历史最高排名 / 当时有效人数</small><strong>${rank(item.bestRank,item.bestTotal)}</strong></div><div><small>历史最高分数</small><strong>${value(item.bestScore,4)}</strong></div></div></article>`}
fetch(`data.json?t=${Date.now()}`,{cache:"no-store"}).then(r=>{if(!r.ok)throw Error();return r.json()}).then(data=>{document.querySelector("#checked-at").textContent=time(data.checkedAt);document.querySelector("#source-time").textContent=time(data.sourcePublishedAt);document.querySelector("#stages").innerHTML=data.stages.map(card).join("");document.querySelector("#status-text").textContent="每小时更新"}).catch(()=>{document.querySelector(".status").classList.add("error");document.querySelector("#status-text").textContent="数据暂不可用";document.querySelector("#checked-at").textContent="无";document.querySelector("#stages").innerHTML=["初赛","复赛","决赛"].map((stage,i)=>card({stage,currentRank:null,currentTotal:null,currentScore:null,bestRank:null,bestTotal:null,bestScore:null},i)).join("")});

